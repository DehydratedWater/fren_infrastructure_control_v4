# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets — NEVER commit
.env
*.env
!.env.example
.google_token.json

# Compiled agent trees + runtime data
/data/
.opencode/
.oac/snapshots/
.oac/promoted/
# Local smoke-test / scratch compiled fleets
.smoke_agents/
*.smoke/
# Scheduler / run state written at runtime
run_logs/
/config/one_time_schedule.yml
data/scheduler_state.json
# OAuth tokens materialised at runtime (any account suffix)
.google_token*.json

# Node (dashboard/frontend)
node_modules/
dist/

# OS / editor cruft
.DS_Store
*.swp
*~
