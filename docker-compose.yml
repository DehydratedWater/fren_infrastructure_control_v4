name: fren-v4

x-backend: &backend
  build:
    context: .
    dockerfile: backend/Dockerfile
  # All credentials + config flow from the gitignored .env (32 keys copied
  # from v3). PYTHONPATH makes the mounted framework importable.
  env_file: .env
  environment:
    PYTHONPATH: /srv/oac:/app:/app/backend
    AGENTS_DIR: ${AGENTS_DIR:-/data/agents}
  depends_on:
    db:
      condition: service_healthy
  volumes:
    - fren_v4_data:/data
    - ../OpenCodeCompilerV2:/srv/oac:ro   # framework as a sibling, importable
  restart: unless-stopped

services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${FREN_DB_PORT:-5452}:5432"
    volumes:
      - fren_v4_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # Primary surface: the Telegram bot (also runs migrations at boot).
  bot:
    <<: *backend
    environment:
      PYTHONPATH: /srv/oac:/app:/app/backend
      AGENTS_DIR: ${AGENTS_DIR:-/data/agents}
      SERVICE: bot

  # Autonomous job runners (cron-driven agents + periodic checker).
  scheduler:
    <<: *backend
    environment:
      PYTHONPATH: /srv/oac:/app:/app/backend
      AGENTS_DIR: ${AGENTS_DIR:-/data/agents}
      SERVICE: scheduler
    depends_on:
      bot:
        condition: service_started

  checker:
    <<: *backend
    environment:
      PYTHONPATH: /srv/oac:/app:/app/backend
      AGENTS_DIR: ${AGENTS_DIR:-/data/agents}
      SERVICE: checker
    depends_on:
      bot:
        condition: service_started

volumes:
  fren_v4_db_data:
  fren_v4_data:
