"""Context tools — retrieval and memory systems for agents.

Ported from fren v3 (src/fren/tools/context/). These ScriptTools expose the
agent-facing memory and context surface: chat history, embeddings/semantic
search, context cache & pins, memory manager, link enrichment/search,
execution ledger, intent inference, response processing, and read-only bridges
to external logs (Telegram activity log, Garmin health).
"""
