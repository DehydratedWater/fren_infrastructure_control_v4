"""System tool area — ported from fren v3.

ScriptTool implementations for system-level operations: cron management and
history, read-only DB queries, agent analysis / notes / lessons, invoice and
schedule management, locks, sessions inspection, screenshots, camera capture,
night analysis, persona memory/vibe, periodic checking, proactive sends,
RALF process management, report/visual report writing, peek-thought, and user
mood. CLI entrypoints live under ``scripts/`` at the repo root.
"""
