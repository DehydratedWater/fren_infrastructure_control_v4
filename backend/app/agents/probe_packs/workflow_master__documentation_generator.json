{
  "agent_id": "workflow_master/documentation_generator",
  "generated_at": "2026-06-09T23:29:51.608676+00:00",
  "source": "v3-corpus",
  "teacher": "glm-5.1",
  "probes": [
    {
      "name": "generate_single_agent_doc",
      "prompt": "Can you generate documentation for the following agent file? Here is the full content:\n\n---\nname: food_suggester\nversion: 1.2.0\ndescription: Suggests meals from user preferences, available ingredients, and past history.\ndepends_on:\n  - retrieval/fast_retrieval\n  - profile/pattern_observer\ntriggers:\n  - workflow_food_suggest\n  - direct_mention\noutputs:\n  - structured_meal_suggestion\n---\n# Food Suggester Agent\n\nThis agent receives a food request and builds a personalised meal suggestion. It queries recent meal history via `retrieval/fast_retrieval`, checks dietary patterns from `profile/pattern_observer`, and ranks candidate meals by match score.\n\n## Key Behaviour\n- Prefers meals not eaten in the last 3 days.\n- Avoids allergens flagged in user profile.\n- Returns up to 3 suggestions with prep-time estimates.",
      "criteria": "The documentation_generator agent should parse the provided YAML frontmatter and markdown body, then output a well-structured per-agent doc page that includes the agent name, version, description, dependencies, triggers, outputs, and a readable summary of key behaviour — formatted with clear headings and possibly a quick-reference table.\nScore 0.0 if the response merely describes its own role, simulates or role-plays a user/conversation, narrates what it 'would' do, refuses, echoes the prompt, or leaks tool/JSON mechanics instead of answering. Reward a concrete, correct, on-task result a user could use as-is.\nIf instead the response is a bracketed note that the agent acted by invoking tools/subagents, judge whether THOSE actions are the right ones for this request: correct delegation or tool use for the role scores high; flailing on wrong, hallucinated, or blocked tools scores low.\nTOOL DISCIPLINE: if the response carries a 'TOOL DISCIPLINE' note that the agent made DENIED/blocked tool attempts (forbidden by its allow-list) or that the session ERRORED, LOWER the score in proportion to the number of blocked attempts, and score an errored session near 0 (a failed run, not an empty answer).",
      "pass_threshold": 0.7,
      "source_hint": "Inspired by messages 7 ('It seems it it works quite nicely') and 16 ('Remind me later to check this workflow') — user asking to inspect/document a specific workflow agent."
    },
    {
      "name": "generate_fleet_overview_table",
      "prompt": "I need an overview doc with quick-reference tables for these three agents. Here are their details:\n\nAgent A: name=twily_chat, description='Lightweight intent-planner that emits structured guidance', version=2.0.1, depends_on=[retrieval/fast_retrieval], triggers=[incoming_message], outputs=[intent_plan].\nAgent B: name=food_suggester, description='Suggests meals from preferences and history', version=1.2.0, depends_on=[retrieval/fast_retrieval, profile/pattern_observer], triggers=[workflow_food_suggest, direct_mention], outputs=[structured_meal_suggestion].\nAgent C: name=hardware_agent, description='Monitors CPU, RAM, temperature, uptime, and docker containers', version=1.0.3, depends_on=[server/orchestrator], triggers=[cron_5min], outputs=[hardware_status_report].\n\nCan you make me a nice overview with tables?",
      "criteria": "The documentation_generator agent should produce an overview document containing at minimum a quick-reference table (or tables) listing each agent's name, description, version, dependencies, triggers, and outputs — cleanly formatted so a reader can compare agents at a glance. A dependency diagram in text or mermaid notation is a bonus.\nScore 0.0 if the response merely describes its own role, simulates or role-plays a user/conversation, narrates what it 'would' do, refuses, echoes the prompt, or leaks tool/JSON mechanics instead of answering. Reward a concrete, correct, on-task result a user could use as-is.\nIf instead the response is a bracketed note that the agent acted by invoking tools/subagents, judge whether THOSE actions are the right ones for this request: correct delegation or tool use for the role scores high; flailing on wrong, hallucinated, or blocked tools scores low.\nTOOL DISCIPLINE: if the response carries a 'TOOL DISCIPLINE' note that the agent made DENIED/blocked tool attempts (forbidden by its allow-list) or that the session ERRORED, LOWER the score in proportion to the number of blocked attempts, and score an errored session near 0 (a failed run, not an empty answer).",
      "pass_threshold": 0.7,
      "source_hint": "Inspired by message 13 ('We need to actually prepare a product renders and spec to test out, then setting it up online should be easy enough') — preparing structured spec/overview material."
    },
    {
      "name": "dependency_diagram_request",
      "prompt": "Wygeneruj mi diagram zależności dla tych agentów, pls. Mam taką listę:\n\n1. server/orchestrator — depends on: server/hardware_agent, server/sessions_agent, server/camera_capture_agent\n2. server/hardware_agent — depends on: niczego\n3. server/sessions_agent — depends on: niczego\n4. server/camera_capture_agent — depends on: server/vision_analyzer\n5. server/vision_analyzer — depends on: niczego\n\nChcę żeby było widać co z czego korzysta, najlepiej jako mermaid diagram.",
      "criteria": "The documentation_generator agent should parse the provided dependency list and render a dependency diagram (ideally in Mermaid syntax) that clearly shows which agents depend on which others. The diagram should be accurate — leaf nodes (no dependencies) should have no outgoing edges, and the orchestrator should fan out to its three dependents with camera_capture_agent further linking to vision_analyzer.\nScore 0.0 if the response merely describes its own role, simulates or role-plays a user/conversation, narrates what it 'would' do, refuses, echoes the prompt, or leaks tool/JSON mechanics instead of answering. Reward a concrete, correct, on-task result a user could use as-is.\nIf instead the response is a bracketed note that the agent acted by invoking tools/subagents, judge whether THOSE actions are the right ones for this request: correct delegation or tool use for the role scores high; flailing on wrong, hallucinated, or blocked tools scores low.\nTOOL DISCIPLINE: if the response carries a 'TOOL DISCIPLINE' note that the agent made DENIED/blocked tool attempts (forbidden by its allow-list) or that the session ERRORED, LOWER the score in proportion to the number of blocked attempts, and score an errored session near 0 (a failed run, not an empty answer).",
      "pass_threshold": 0.7,
      "source_hint": "Inspired by message 3 ('Can you fetch me a link to that video?') and the Polish/English mix in the corpus. Also message 24 ('add subsystem for tracking evolving meta') — visualising structure."
    },
    {
      "name": "detect_missing_frontmatter",
      "prompt": "I pasted this agent file below. Something feels off about the docs — can you check it and generate proper documentation, flagging anything that's missing or broken?\n\n---\nname: priority_auditor\nversion: 0.9.0\n---\n# Priority Auditor\n\nAudits priority real-importance scores against actual user behaviour. Uses data from `retrieval/fast_retrieval` and `profile/pattern_observer`.\n\nProduces a scored audit report.",
      "criteria": "The documentation_generator agent should parse the YAML and markdown, note that fields like 'description', 'depends_on', 'triggers', and 'outputs' are absent from the frontmatter, explicitly flag these gaps, and still produce the best possible per-agent doc from available information. The response should mention what is missing and suggest what the completed frontmatter should look like.\nScore 0.0 if the response merely describes its own role, simulates or role-plays a user/conversation, narrates what it 'would' do, refuses, echoes the prompt, or leaks tool/JSON mechanics instead of answering. Reward a concrete, correct, on-task result a user could use as-is.\nIf instead the response is a bracketed note that the agent acted by invoking tools/subagents, judge whether THOSE actions are the right ones for this request: correct delegation or tool use for the role scores high; flailing on wrong, hallucinated, or blocked tools scores low.\nTOOL DISCIPLINE: if the response carries a 'TOOL DISCIPLINE' note that the agent made DENIED/blocked tool attempts (forbidden by its allow-list) or that the session ERRORED, LOWER the score in proportion to the number of blocked attempts, and score an errored session near 0 (a failed run, not an empty answer).",
      "pass_threshold": 0.7,
      "source_hint": "Inspired by messages 5 ('Dates when, and what is requried are crutial') and 31 ('hey, but epit is already done, can you mark it as done?') — user cares about completeness and correctness of metadata."
    },
    {
      "name": "mixed_language_multi_agent_doc",
      "prompt": "Mam trzy pliki agentów do udokumentowania. Wygeneruj per-agent docs i krótkie podsumowanie. Oto zawartość:\n\nFile 1:\n---\nname: stt_processor\nversion: 1.1.0\ndescription: Cleans speech transcriptions and translates Polish to English.\ndepends_on: []\ntriggers: [voice_message_received]\noutputs: [cleaned_transcript]\n---\n# STT Processor\nReceives raw STT output, removes filler words, corrects Polish diacritics, and optionally translates to English.\n\nFile 2:\n---\nname: tts_formatter\nversion: 1.0.0\ndescription: Rewrites text into natural spoken form for TTS.\ndepends_on: [stt_processor]\ntriggers: [send_voice_reply]\noutputs: [tts_ready_text]\n---\n# TTS Formatter\nTakes agent output text and rewrites it so text-to-speech sounds natural — adds pauses, expands abbreviations.\n\nFile 3:\n---\nname: email_agent\nversion: 2.3.1\ndescription: Gmail operations with a draft-then-send safety gate.\ndepends_on: [retrieval/fast_retrieval]\ntriggers: [workflow_email, direct_mention]\noutputs: [email_draft, sent_confirmation]\n---\n# Email Agent\nHandles reading, searching, composing and sending emails via Gmail. Uses a draft-then-send safety gate — nothing is sent without user confirmation.",
      "criteria": "The documentation_generator agent should produce three separate per-agent documentation sections (one for each file) plus a brief overview summary with a quick-reference table. Each per-agent doc should capture name, version, description, dependencies, triggers, outputs, and a summary paragraph. The overview should highlight the dependency chain (e.g., tts_formatter depends on stt_processor). Language of the response may mix English and Polish as the user wrote in Polish.\nScore 0.0 if the response merely describes its own role, simulates or role-plays a user/conversation, narrates what it 'would' do, refuses, echoes the prompt, or leaks tool/JSON mechanics instead of answering. Reward a concrete, correct, on-task result a user could use as-is.\nIf instead the response is a bracketed note that the agent acted by invoking tools/subagents, judge whether THOSE actions are the right ones for this request: correct delegation or tool use for the role scores high; flailing on wrong, hallucinated, or blocked tools scores low.\nTOOL DISCIPLINE: if the response carries a 'TOOL DISCIPLINE' note that the agent made DENIED/blocked tool attempts (forbidden by its allow-list) or that the session ERRORED, LOWER the score in proportion to the number of blocked attempts, and score an errored session near 0 (a failed run, not an empty answer).",
      "pass_threshold": 0.7,
      "source_hint": "Inspired by messages 8 ('Yes, please send it, just add some fun jocke at the end'), 17 ('I got out for a walk'), and the general Polish/English code-switching pattern in the corpus."
    }
  ]
}